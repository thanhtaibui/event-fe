export interface EventPayload {
  title: string;
  eventPoster: string;
  startDateTime: string;
  endDateTime: string;
  registrationEndDate: string;
  capacity: number;
  organizationId: string;
  description: string;
  place: string;
}
